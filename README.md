# setup-zsh

Installs zsh, Oh My Zsh, and a set of plugins (autosuggestions, syntax highlighting, autocomplete), then wires them into `.zshrc`.

## Run

```sh
curl -fsSL https://raw.githubusercontent.com/nhamtphat/setup-zsh/main/setup-zsh.sh | bash
```
